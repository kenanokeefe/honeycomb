app.service('Look', ['$rootScope', 'Element', function($rootScope, Element) {

	var Look = {}

	Look.leftClick = function(snap) {
	}

	Look.hover = function($event) { 
	}

	return Look;

}]);

//use marquee to select multiple
//can also just click one