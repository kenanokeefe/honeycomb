app.service('MirrorEvent', ['$rootScope', 'Element', function($rootScope, Element) {

	var MirrorEvent = {}

	MirrorEvent.leftClick = function(snap) {
		//have to select to lines
		//have to select MirrorEvent line
	}

	MirrorEvent.render = function(pendings, snap) {
		//copy elements
		//create a MirrorEvent matrix to use...
	}

	return MirrorEvent;

}]);

/*

idea - if reference line is just hanging by itself, change it to a construction line...

*/